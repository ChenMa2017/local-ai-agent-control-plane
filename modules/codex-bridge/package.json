{
  "name": "codex-bridge",
  "displayName": "Codex Bridge",
  "description": "Local-first bridge core that turns external messages into safe, logged Codex tasks.",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "bin": {
    "codex-bridge": "./scripts/codex-bridge.js"
  },
  "scripts": {
    "bridge": "node scripts/codex-bridge.js",
    "bridge:demo": "node scripts/codex-bridge.js run --project self --dry-run \"请模拟检查这个项目当前状态\"",
    "check": "node --check scripts/codex-bridge.js",
    "test": "node tests/lifecycle.test.js && node tests/safe-output.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
